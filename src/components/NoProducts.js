import React from "react";

const NoProducts = () => {
  return (
    <div className="alert alert-warning">
      There are no Products to display. Enter an ASIN in the form above to get
      started.
    </div>
  );
};

export default NoProducts;
